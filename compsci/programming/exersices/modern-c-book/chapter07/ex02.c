#include <stdio.h>

int main(void) {

  char text;
  int value;

  printf("Enter a word: ");
  while(1) {
    scanf("%c", &text);

    if (text == 'a' || text == 'b' || text == 'c' || text == 'b' )
    else if (text == '\n') {
      break;
    }

  }


  return 0;
}
